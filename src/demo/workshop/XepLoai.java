package demo.workshop;

/**
 * Đây là enum XepLoai đại diện cho các mức độ xếp loại khác nhau của nhân viên.
 */
public enum XepLoai {

    /**
     * Mức xếp loại A, đại diện cho mức độ hoàn thành công việc cao nhất.
     */
    A,

    /**
     * Mức xếp loại B, đại diện cho mức độ hoàn thành công việc trung bình.
     */
    B,

    /**
     * Mức xếp loại C, đại diện cho mức độ hoàn thành công việc thấp nhất.
     */
    C
}
